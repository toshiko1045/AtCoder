#include <bits/stdc++.h>
using namespace std;

int N,L,M1,M2;
vector<int> a;

int main(){
    cin >> N >> L;
    a = vector<int>(N);
    for(int i=0;i<N;i++) cin >> a.at(i);

    M = *max_element(a.begin(),a.end());

}